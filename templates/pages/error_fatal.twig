{% set content %}
  <div class="fatal_error">
    {{ context.error_message|raw }}
    <div>
      <a href="javascript:history.go(-1)">{{ txt.back }}</a>
    </div>
  </div>
{% endset %}

{% include 'components/generic/article.twig' with {'title': context.error_title, 'content': content} %}
